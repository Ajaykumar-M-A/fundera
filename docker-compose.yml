services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fundera
      POSTGRES_PASSWORD: fundera
      POSTGRES_DB: fundera
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fundera"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://fundera:fundera@db:5432/fundera
      SECRET_KEY: dev-secret-change-in-production
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
